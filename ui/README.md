# LiqVid UI

This is the React frontend for the LiqVid 3D Computational Fluid Dynamics (CFD) engine.

## Setup & Running

```bash
npm install
npm run dev
```

The frontend connects to the Rust backend (which should be running concurrently) to submit and visualize 3D fluid simulations.
